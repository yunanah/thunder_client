import React from 'react';
import CertPresenter from './CertPresenter';

const CertContainer = () => {
  // 각종 변수나 함수
  const handlePressNextButton = () => {};

  return <CertPresenter onPressNextButton={handlePressNextButton} />;
};

export default CertContainer;
